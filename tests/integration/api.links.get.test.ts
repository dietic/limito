import { describe, it, expect } from 'vitest'

// Placeholder integration test — clean slate; proper harness coming soon.
describe('api/links integration (placeholder)', () => {
  it('always passes', () => {
    expect(true).toBe(true)
  })
})
